import { IUtilsActive } from "../types/types";

const UTILS_ACTIVE: IUtilsActive = {
  event: true,
  unratedSpoilers: true,
  graphs: true,
  redirectOnCard: true,
  topsBtns: true,
  favEvents: true,
  refreshEvent: true,
};

export default UTILS_ACTIVE;
